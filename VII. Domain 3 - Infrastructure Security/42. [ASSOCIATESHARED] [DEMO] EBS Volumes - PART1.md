# EBS Volumes Demonstration — Part 1

## Overview

This lesson explains EBS Volumes Demonstration — Part 1 as an infrastructure-security control. It converts the source lesson into the architecture, decision points, and failure modes most relevant to AWS Certified Security – Specialty (SCS-C03).

The central security objective is to create an intentional network or data path, apply controls at the correct layer, and retain enough evidence to distinguish routing, filtering, identity, encryption, and service-configuration failures.

## Exam Alignment

- **Primary SCS-C03 Domain:** Infrastructure Security
- **Secondary Domain(s):** Data Protection
- **Main AWS Services:** Amazon EBS, Amazon EC2
- **Lesson Type:** Demonstration
- **Exam Importance:** High
- **Core Skill:** Explain, configure, and troubleshoot EBS Volumes Demonstration — Part 1 without confusing reachability with authorization.

## Core Concepts

- Create an EBS volume in the same Availability Zone as the source EC2 instance and attach it.
- Identify the correct block device, create a filesystem only on a new empty volume, mount it, and write a test file.
- Persist mounts with filesystem identifiers rather than fragile device names when required.
- This part produces a volume containing verifiable data for migration in Part 2.

## How It Works

1. Create an EBS volume in the same Availability Zone as the source EC2 instance and attach it.
2. Identify the correct block device, create a filesystem only on a new empty volume, mount it, and write a test file.
3. Persist mounts with filesystem identifiers rather than fragile device names when required.
4. This part produces a volume containing verifiable data for migration in Part 2.

## Architecture and Service Relationships

```text
Initiating principal or workload
        |
        v
Network path / Amazon EBS
        |
        v
Policy and traffic controls
        |
        v
Target resource and security evidence
```

The route or service attachment establishes a possible path. Resource policies, identity policies, security groups, network ACLs, encryption controls, and application authorization remain independently enforceable where applicable.

## Demonstration Goal

Implement and validate the current stage of EBS Volumes Demonstration — Part 1 while preserving the boundaries established by earlier stages.

## Prerequisites

- An AWS account and permissions to configure Amazon EBS, Amazon EC2.
- The VPC, CIDR plan, identities, keys, and prior-stage resources named by the source lesson.
- A selected AWS Region and nonoverlapping address ranges where connectivity is involved.
- A rollback or cleanup plan for billable resources.

## Demonstration Steps

1. Create an EBS volume in the same Availability Zone as the source EC2 instance and attach it. This step supplies a control or dependency that must be validated before continuing.
2. Identify the correct block device, create a filesystem only on a new empty volume, mount it, and write a test file. This step supplies a control or dependency that must be validated before continuing.
3. Persist mounts with filesystem identifiers rather than fragile device names when required. This step supplies a control or dependency that must be validated before continuing.
4. This part produces a volume containing verifiable data for migration in Part 2. This step supplies a control or dependency that must be validated before continuing.

Use the device name discovered on the instance; Nitro-based instances may expose an attached EBS volume under an NVMe name rather than the attachment name shown by the EC2 API.

```bash
lsblk -f
sudo file -s /dev/nvme1n1
sudo mkfs -t xfs /dev/nvme1n1
sudo mkdir -p /ebs-test
sudo mount /dev/nvme1n1 /ebs-test
sudo sh -c 'printf "%s\n" "verification data" > /ebs-test/amazingtestfile.txt'
```

### Line-by-Line Comments

```text
Line 1: Lists block devices, filesystems, labels, UUIDs, and mount points.
Line 2: Checks whether the selected device already contains recognizable data or a filesystem.
Line 3: Creates a new XFS filesystem; this is destructive and is valid only for the new empty lab volume.
Line 4: Creates the mount-point directory.
Line 5: Mounts the filesystem for the current boot.
Line 6: Writes a non-sensitive validation file as root.
```

### Code Walkthrough

These operating-system commands require administrative privileges on the instance; they do not call AWS APIs. `lsblk` and `file` are read-only. `mkfs` destroys existing filesystem metadata, `mkdir` changes the instance filesystem, `mount` changes runtime mount state, and the final command writes data. Never copy the example device name without matching it to `lsblk`. Success is a mounted XFS filesystem containing the validation file.

## Validation

- Confirm the expected resource state for Amazon EBS, Amazon EC2 in the selected Region.
- Test the intended success path and at least one expected-denial path.
- Confirm that traffic uses the intended private, encrypted, or inspected path rather than an unintended fallback.
- Review relevant service status, route tables, policy evaluation, and logs when the result differs from expectations.

## Cleanup

Remove the resources identified by the source lab when they are no longer needed. Respect dependency order, retain evidence required by policy, and confirm that billable endpoints, gateways, instances, addresses, or data copies are not left behind.

## What the Demonstration Proves

The exercise proves that eBS attachment is AZ-local; filesystem operations are separate from volume attachment. It also shows that a successful console configuration is not enough: effective routes, policies, filters, and observed behavior must agree.

## Detection, Logging, and Evidence

AWS CloudTrail records Amazon EC2, Amazon EBS, and AWS KMS API activity, including volume, snapshot, key, principal, Region, and error context. AWS Config can evaluate configuration state, while Amazon CloudWatch exposes volume performance. Correlate these sources when distinguishing a permissions problem from key state, attachment state, or storage performance.

## Troubleshooting

| Symptom | Likely Cause | Verification | Resolution |
| --- | --- | --- | --- |
| Encrypted volume cannot be attached or used | The instance role or service path cannot use the KMS key, or the key is disabled | Inspect the EC2 and KMS CloudTrail events and key state | Correct the key policy or grant and ensure the key is enabled |
| Snapshot copy fails | The source key cannot be used or the destination key/Region is incorrect | Review snapshot-copy status and KMS permissions in both Regions | Grant the required decrypt/encrypt path and select the intended destination key |
| Volume is attached but the file system is unavailable | Operating-system mount, device mapping, or file-system issue | Inspect EC2 device and OS state | Correct the OS-level configuration without recreating encryption controls |
| I/O is slow | Volume performance limits or instance bandwidth, not encryption itself | Review EBS and EC2 CloudWatch metrics | Select or configure storage and instance performance for the workload |
## Security Best Practices

- Apply least privilege to management and data-plane access for Amazon EBS, Amazon EC2.
- Prefer private paths and managed controls when they meet the requirement, but validate both routing and authorization.
- Design across Availability Zones where the service is zonal and the workload requires resilience.
- Log configuration changes and network behavior; alert on unauthorized or high-risk changes.
- Test an explicit denial path so an unnoticed public or uninspected fallback is not mistaken for success.

## Exam Decision Guide

| Requirement or Clue | Most Likely Control | Why |
| --- | --- | --- |
| Implement the exact capability taught here | Amazon EBS | It directly supplies the lesson's control point |
| Determine who changed the configuration | AWS CloudTrail | It records supported API activity and identity context |
| Enforce application-layer HTTP request rules | AWS WAF | It evaluates web requests rather than general VPC packet flows |

## Common Exam Traps

- Do not assume that creating Amazon EBS automatically updates routes, policies, DNS, or filters unless the service explicitly performs that integration.
- Network reachability is not IAM authorization, and an IAM allow cannot repair a missing route.
- Logging observes activity; it does not automatically prevent or remediate it.
- Regional, Availability Zone, and global scope matter. Place dependencies in the scope required by the service.
- Explicit deny overrides an applicable allow; default behavior is implicit deny where IAM authorization applies.

## Memory Hooks

### One-Line Memory Hook

> **EBS attachment is AZ-local; filesystem operations are separate from volume attachment.**

### Decision Memory Hook

> **No path? Check routes and DNS. Path but no packets? Check filters. Packets reach the service but fail? Check authorization and encryption dependencies.**

## Key Terms and Definitions

| Term | Definition | Exam Relevance |
| --- | --- | --- |
| Amazon EBS | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |
| Amazon EC2 | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |

## Quick Review

- Create an EBS volume in the same Availability Zone as the source EC2 instance and attach it.
- Identify the correct block device, create a filesystem only on a new empty volume, mount it, and write a test file.
- Persist mounts with filesystem identifiers rather than fragile device names when required.
- This part produces a volume containing verifiable data for migration in Part 2.
- EBS attachment is AZ-local; filesystem operations are separate from volume attachment.
- CloudTrail identifies configuration and key API activity; CloudWatch helps distinguish storage-performance symptoms.

## Self-Check Questions

1. What security problem does EBS Volumes Demonstration — Part 1 solve?

<details>
<summary>Answer</summary>

It provides the control described by the lesson: create an EBS volume in the same Availability Zone as the source EC2 instance and attach it.

</details>

2. What is the most important exam distinction for this lesson?

<details>
<summary>Answer</summary>

EBS attachment is AZ-local; filesystem operations are separate from volume attachment. The surrounding route, policy, and filtering dependencies must also be evaluated.

</details>

3. A configured resource exists but the test still fails. What should be checked next?

<details>
<summary>Answer</summary>

Check DNS resolution, forward and return routes, security groups, network ACLs, applicable IAM/resource/key policies, service state, and logs. A resource's existence proves neither reachability nor authorization.

</details>

## References

### Source References

- https://learn.cantrill.io/courses/1723753/lectures/40965801

### Current AWS References

- https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html

---

## Updated Information as of 2026-09-07

> No material technical updates were required for this lesson. Terminology and formatting were standardized for SCS-C03 study use.
