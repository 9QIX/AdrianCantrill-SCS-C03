# Public Subnets and Bastion Host Demonstration — Part 2

## Overview

This lesson explains Public Subnets and Bastion Host Demonstration — Part 2 as an infrastructure-security control. It converts the source lesson into the architecture, decision points, and failure modes most relevant to AWS Certified Security – Specialty (SCS-C03).

The central security objective is to create an intentional network or data path, apply controls at the correct layer, and retain enough evidence to distinguish routing, filtering, identity, encryption, and service-configuration failures.

## Exam Alignment

- **Primary SCS-C03 Domain:** Infrastructure Security
- **Secondary Domain(s):** None
- **Main AWS Services:** Amazon VPC, Amazon EC2, AWS Systems Manager
- **Lesson Type:** Demonstration
- **Exam Importance:** High
- **Core Skill:** Explain, configure, and troubleshoot Public Subnets and Bastion Host Demonstration — Part 2 without confusing reachability with authorization.

## Core Concepts

- Launch the bastion in a public subnet with public IPv4 addressing.
- Limit SSH or RDP ingress to the administrator source address and restrict egress where operationally practical.
- Verify the route, public address, network ACL, and security group as separate dependencies.
- Treat the bastion as a legacy learning pattern; prefer Session Manager when requirements permit.

## How It Works

1. Launch the bastion in a public subnet with public IPv4 addressing.
2. Limit SSH or RDP ingress to the administrator source address and restrict egress where operationally practical.
3. Verify the route, public address, network ACL, and security group as separate dependencies.
4. Treat the bastion as a legacy learning pattern; prefer Session Manager when requirements permit.

## Architecture and Service Relationships

```text
Initiating principal or workload
        |
        v
Network path / Amazon VPC
        |
        v
Policy and traffic controls
        |
        v
Target resource and security evidence
```

The route or service attachment establishes a possible path. Resource policies, identity policies, security groups, network ACLs, encryption controls, and application authorization remain independently enforceable where applicable.

## Demonstration Goal

Implement and validate the current stage of Public Subnets and Bastion Host Demonstration — Part 2 while preserving the boundaries established by earlier stages.

## Prerequisites

- An AWS account and permissions to configure Amazon VPC, Amazon EC2, AWS Systems Manager.
- The VPC, CIDR plan, identities, keys, and prior-stage resources named by the source lesson.
- A selected AWS Region and nonoverlapping address ranges where connectivity is involved.
- A rollback or cleanup plan for billable resources.

## Demonstration Steps

1. Launch the bastion in a public subnet with public IPv4 addressing. This step supplies a control or dependency that must be validated before continuing.
2. Limit SSH or RDP ingress to the administrator source address and restrict egress where operationally practical. This step supplies a control or dependency that must be validated before continuing.
3. Verify the route, public address, network ACL, and security group as separate dependencies. This step supplies a control or dependency that must be validated before continuing.
4. Treat the bastion as a legacy learning pattern; prefer Session Manager when requirements permit. This step supplies a control or dependency that must be validated before continuing.

## Validation

- Confirm the expected resource state for Amazon VPC, Amazon EC2, AWS Systems Manager in the selected Region.
- Test the intended success path and at least one expected-denial path.
- Confirm that traffic uses the intended private, encrypted, or inspected path rather than an unintended fallback.
- Review relevant service status, route tables, policy evaluation, and logs when the result differs from expectations.

## Cleanup

Remove the resources identified by the source lab when they are no longer needed. Respect dependency order, retain evidence required by policy, and confirm that billable endpoints, gateways, instances, addresses, or data copies are not left behind.

## What the Demonstration Proves

The exercise proves that public IP without a gateway route is not public connectivity; a gateway route without an address is not enough either. It also shows that a successful console configuration is not enough: effective routes, policies, filters, and observed behavior must agree.

## Detection, Logging, and Evidence

Use AWS CloudTrail for control-plane changes to Amazon VPC. For packet-path questions, enable VPC Flow Logs on the relevant interfaces, subnets, or VPC; flow logs show accepted or rejected IP flows but do not capture packet payloads. Combine service status and metrics with route-table, DNS, policy, and firewall configuration evidence.

## Troubleshooting

| Symptom | Likely Cause | Verification | Resolution |
| --- | --- | --- | --- |
| Connection times out | Missing route, return path, or filtering rule | Inspect effective routes, security groups, network ACLs, and Flow Logs | Restore symmetric routing and least-privilege traffic rules |
| Access is denied | Identity, resource, endpoint, key, or organization guardrail denies the request | Inspect CloudTrail error context and all applicable policies | Add the required allow or remove the unintended explicit deny |
| Expected private path is unused | DNS or a more-specific route selects another destination | Resolve the hostname and compare the route table with destination addresses | Correct private DNS, endpoint associations, or routes |
| Works in one Availability Zone only | Zonal resource, association, or return path is missing | Compare endpoint/gateway placement and route tables per AZ | Deploy and associate the required zonal components |

## Security Best Practices

- Apply least privilege to management and data-plane access for Amazon VPC, Amazon EC2, AWS Systems Manager.
- Prefer private paths and managed controls when they meet the requirement, but validate both routing and authorization.
- Design across Availability Zones where the service is zonal and the workload requires resilience.
- Log configuration changes and network behavior; alert on unauthorized or high-risk changes.
- Test an explicit denial path so an unnoticed public or uninspected fallback is not mistaken for success.

## Exam Decision Guide

| Requirement or Clue | Most Likely Control | Why |
| --- | --- | --- |
| Implement the exact capability taught here | Amazon VPC | It directly supplies the lesson's control point |
| Determine who changed the configuration | AWS CloudTrail | It records supported API activity and identity context |
| Determine whether IP traffic was accepted or rejected | VPC Flow Logs | It records flow metadata at supported VPC collection points |
| Enforce application-layer HTTP request rules | AWS WAF | It evaluates web requests rather than general VPC packet flows |

## Common Exam Traps

- Do not assume that creating Amazon VPC automatically updates routes, policies, DNS, or filters unless the service explicitly performs that integration.
- Network reachability is not IAM authorization, and an IAM allow cannot repair a missing route.
- Logging observes activity; it does not automatically prevent or remediate it.
- Regional, Availability Zone, and global scope matter. Place dependencies in the scope required by the service.
- Explicit deny overrides an applicable allow; default behavior is implicit deny where IAM authorization applies.

## Memory Hooks

### One-Line Memory Hook

> **Public IP without a gateway route is not public connectivity; a gateway route without an address is not enough either.**

### Decision Memory Hook

> **No path? Check routes and DNS. Path but no packets? Check filters. Packets reach the service but fail? Check authorization and encryption dependencies.**

## Key Terms and Definitions

| Term | Definition | Exam Relevance |
| --- | --- | --- |
| Amazon VPC | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |
| Amazon EC2 | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |
| AWS Systems Manager | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |
| Longest-prefix match | Routing selects the most specific matching destination prefix. | A specific service or network route can beat a default route. |

## Quick Review

- Launch the bastion in a public subnet with public IPv4 addressing.
- Limit SSH or RDP ingress to the administrator source address and restrict egress where operationally practical.
- Verify the route, public address, network ACL, and security group as separate dependencies.
- Treat the bastion as a legacy learning pattern; prefer Session Manager when requirements permit.
- Public IP without a gateway route is not public connectivity; a gateway route without an address is not enough either.
- CloudTrail answers who changed configuration; VPC Flow Logs help diagnose IP flow acceptance and rejection.

## Self-Check Questions

1. What security problem does Public Subnets and Bastion Host Demonstration — Part 2 solve?

<details>
<summary>Answer</summary>

It provides the control described by the lesson: launch the bastion in a public subnet with public IPv4 addressing.

</details>

2. What is the most important exam distinction for this lesson?

<details>
<summary>Answer</summary>

Public IP without a gateway route is not public connectivity; a gateway route without an address is not enough either. The surrounding route, policy, and filtering dependencies must also be evaluated.

</details>

3. A configured resource exists but the test still fails. What should be checked next?

<details>
<summary>Answer</summary>

Check DNS resolution, forward and return routes, security groups, network ACLs, applicable IAM/resource/key policies, service state, and logs. A resource's existence proves neither reachability nor authorization.

</details>

## References

### Source References

- https://learn.cantrill.io/courses/1723753/lectures/41924001

### Current AWS References

- https://docs.aws.amazon.com/vpc/latest/userguide/infrastructure-security.html

---

## Updated Information as of 2026-09-07

| Original or Older Information | Current Information | Reason for Update | Exam Impact |
| --- | --- | --- | --- |
| Bastion-host administration was presented as the primary private-instance access pattern. | AWS Systems Manager Session Manager is generally preferred when supported because it avoids inbound administration ports and distributed SSH keys. | Managed access reduces attack surface and improves auditable access. | Choose Session Manager when the clue emphasizes no inbound ports or managed access. |
