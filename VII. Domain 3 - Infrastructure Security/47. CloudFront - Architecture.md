# Amazon CloudFront Architecture

## Overview

This lesson explains Amazon CloudFront Architecture as an infrastructure-security control. It converts the source lesson into the architecture, decision points, and failure modes most relevant to AWS Certified Security – Specialty (SCS-C03).

The central security objective is to create an intentional network or data path, apply controls at the correct layer, and retain enough evidence to distinguish routing, filtering, identity, encryption, and service-configuration failures.

## Exam Alignment

- **Primary SCS-C03 Domain:** Infrastructure Security
- **Secondary Domain(s):** Data Protection
- **Main AWS Services:** Amazon CloudFront, Amazon Route 53
- **Lesson Type:** Architecture
- **Exam Importance:** High
- **Core Skill:** Explain, configure, and troubleshoot Amazon CloudFront Architecture without confusing reachability with authorization.

## Core Concepts

- CloudFront is a global content delivery network that serves viewer requests from edge locations and caches according to behaviors and cache policies.
- A distribution maps viewer hostnames and paths to origins such as S3, load balancers, or custom HTTP servers.
- Cache hits reduce origin load; misses and expired objects trigger origin requests.
- Viewer security, edge authorization, origin protection, logging, AWS WAF, and DDoS resilience are separate layers.

## How It Works

1. CloudFront is a global content delivery network that serves viewer requests from edge locations and caches according to behaviors and cache policies.
2. A distribution maps viewer hostnames and paths to origins such as S3, load balancers, or custom HTTP servers.
3. Cache hits reduce origin load; misses and expired objects trigger origin requests.
4. Viewer security, edge authorization, origin protection, logging, AWS WAF, and DDoS resilience are separate layers.

## Architecture and Service Relationships

```text
Initiating principal or workload
        |
        v
Network path / Amazon CloudFront
        |
        v
Policy and traffic controls
        |
        v
Target resource and security evidence
```

The route or service attachment establishes a possible path. Resource policies, identity policies, security groups, network ACLs, encryption controls, and application authorization remain independently enforceable where applicable.

## Detection, Logging, and Evidence

CloudTrail records CloudFront configuration API activity. Standard or real-time CloudFront logs provide viewer-request evidence when enabled, while CloudWatch metrics expose error rates and cache behavior. Origin logs and AWS WAF logs add origin-side and web-rule context. VPC Flow Logs do not describe viewer traffic handled at CloudFront edge locations.

## Troubleshooting

| Symptom | Likely Cause | Verification | Resolution |
| --- | --- | --- | --- |
| Viewer receives `403` | Geographic restriction, AWS WAF, signed-request validation, origin access, or application authorization denied the request | Correlate CloudFront, AWS WAF, and origin logs | Correct the specific policy, signature, or origin permission |
| Viewer receives `502` or TLS error | Origin DNS, certificate, protocol, port, or TLS compatibility problem | Inspect CloudFront error details and test the origin TLS endpoint | Correct the origin name, certificate, protocol, or approved TLS settings |
| Updated content is not visible | Cached object or cache-key behavior does not reflect the intended variation | Inspect cache policy, object TTL, headers, cookies, and query strings | Correct the cache policy or invalidate content when operationally justified |
| Origin remains directly reachable | Origin access control or custom-origin network restriction is incomplete | Test the origin directly and inspect its resource or network policy | Restrict the origin to the intended CloudFront access path |
## Security Best Practices

- Apply least privilege to management and data-plane access for Amazon CloudFront, Amazon Route 53.
- Prefer private paths and managed controls when they meet the requirement, but validate both routing and authorization.
- Design across Availability Zones where the service is zonal and the workload requires resilience.
- Log configuration changes and network behavior; alert on unauthorized or high-risk changes.
- Test an explicit denial path so an unnoticed public or uninspected fallback is not mistaken for success.

## Exam Decision Guide

| Requirement or Clue | Most Likely Control | Why |
| --- | --- | --- |
| Implement the exact capability taught here | Amazon CloudFront | It directly supplies the lesson's control point |
| Determine who changed the configuration | AWS CloudTrail | It records supported API activity and identity context |
| Enforce application-layer HTTP request rules | AWS WAF | It evaluates web requests rather than general VPC packet flows |

## Common Exam Traps

- Do not assume that creating Amazon CloudFront automatically updates routes, policies, DNS, or filters unless the service explicitly performs that integration.
- Network reachability is not IAM authorization, and an IAM allow cannot repair a missing route.
- Logging observes activity; it does not automatically prevent or remediate it.
- Regional, Availability Zone, and global scope matter. Place dependencies in the scope required by the service.
- Explicit deny overrides an applicable allow; default behavior is implicit deny where IAM authorization applies.

## Memory Hooks

### One-Line Memory Hook

> **Viewer → edge → origin; secure each hop and prevent origin bypass.**

### Decision Memory Hook

> **No path? Check routes and DNS. Path but no packets? Check filters. Packets reach the service but fail? Check authorization and encryption dependencies.**

## Key Terms and Definitions

| Term | Definition | Exam Relevance |
| --- | --- | --- |
| Amazon CloudFront | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |
| Amazon Route 53 | AWS capability used in this lesson's security architecture. | Know its scope, control point, and nearest alternatives. |

## Quick Review

- CloudFront is a global content delivery network that serves viewer requests from edge locations and caches according to behaviors and cache policies.
- A distribution maps viewer hostnames and paths to origins such as S3, load balancers, or custom HTTP servers.
- Cache hits reduce origin load; misses and expired objects trigger origin requests.
- Viewer security, edge authorization, origin protection, logging, AWS WAF, and DDoS resilience are separate layers.
- Viewer → edge → origin; secure each hop and prevent origin bypass.
- CloudTrail identifies configuration activity; CloudFront logs and metrics describe viewer requests and delivery outcomes.

## Self-Check Questions

1. What security problem does Amazon CloudFront Architecture solve?

<details>
<summary>Answer</summary>

It provides the control described by the lesson: Amazon CloudFront is a global content delivery network that serves viewer requests from edge locations and caches according to behaviors and cache policies.

</details>

2. What is the most important exam distinction for this lesson?

<details>
<summary>Answer</summary>

Viewer → edge → origin; secure each hop and prevent origin bypass. The surrounding route, policy, and filtering dependencies must also be evaluated.

</details>

3. A configured resource exists but the test still fails. What should be checked next?

<details>
<summary>Answer</summary>

Check DNS resolution, forward and return routes, security groups, network ACLs, applicable IAM/resource/key policies, service state, and logs. A resource's existence proves neither reachability nor authorization.

</details>

## References

### Source References

- https://learn.cantrill.io/courses/1723753/lectures/39153046

### Current AWS References

- https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/SecurityAndPrivateContent.html

---

## Updated Information as of 2026-09-07

> No material technical updates were required for this lesson. Terminology and formatting were standardized for SCS-C03 study use.
